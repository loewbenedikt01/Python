import pandas as pd


def compute_risk_appetite_regime(equity_df, bond_df, forex_df,
                                 commodity_df, crypto_df, sentiment_df) -> pd.DataFrame:
    return pd.DataFrame()
